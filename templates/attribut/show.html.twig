{% extends 'base.html.twig' %}

{% block title %}Attribut{% endblock %}

{% block body %}
<div class="card elevation-2">
    <div class="card-header">
        <h6 class="card-title">Visualisation de Attribut</h6>
    </div>
    <div class="card-body">
        <dl class="row">
                            <dt class="col-sm-6 text-right">Id :</dt>
                <dd class="col-sm-6 text-secondary">{{ attribut.id }}</dd>
                            <dt class="col-sm-6 text-right">Nom :</dt>
                <dd class="col-sm-6 text-secondary">{{ attribut.nom }}</dd>
                    </dl>
    </div>
    <div class="card-footer text-center d-flex justify-content-center">
        <a class="btn btn-outline-secondary mr-2" href="{{ path('attribut_liste') }}">Retour</a>
        <a class="btn btn-outline-warning mr-2" href="{{ path('attribut_modifier', {'id': attribut.id}) }}">Modifier</a>
        {{ include('attribut/_delete_form.html.twig') }}
    </div>

{% endblock %}
