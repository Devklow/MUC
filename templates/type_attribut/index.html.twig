{% extends 'base.html.twig' %}

{% block title %}TypeAttribut index{% endblock %}

{% block body %}
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <div class="row">
            <div class="col-md-6 text-left"><h6 class="mt-2 font-weight-bold text-primary">Liste des TypeAttribut</h6></div>
            <div class="col-md-6 text-right">
                <a class="btn btn-sm btn-outline-primary" href="{{ path('type_attribut_nouveau') }}">Créer un(e) TypeAttribut</a>
            </div>
        </div>
    </div>
    <div class="card-body">
        <table class="table datatable table-bordered">
            <thead>
                <tr>
                        <th class="text-center">Id</th>
                        <th class="text-center">Nom</th>
                        <th class="text-center" data-orderable="false">Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for type_attribut in type_attributs %}
                <tr>
                        <td class="text-center">{{ type_attribut.id }}</td>
                        <td class="text-center">{{ type_attribut.nom }}</td>
                        <td class="text-center">
                        <a href="{{ path('type_attribut_visualiser', {'id': type_attribut.id}) }}"><i class="fa fa-search text-primary" data-toggle="tooltip" title="Voir"></i></a>
                        <a href="{{ path('type_attribut_modifier', {'id': type_attribut.id}) }}"><i class="fa fa-edit text-warning ml-2 mr-2" data-toggle="tooltip" title='Modifier'></i></a>
                        {{ include('type_attribut/_delete_form.html.twig', {liste:true}) }}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
<div class="col-sm-12 text-center">
    <a class="btn btn-outline-primary" href="{{ path('type_attribut_nouveau') }}">Créer un(e) TypeAttribut</a>
</div>
{{ include('fragments/main-confirm-modal.html.twig', {liste:true}) }}
{% endblock %}

{% block javascripts %}
{{ include('fragments/data-tables.html.twig') }}
{% endblock %}