{% extends 'base.html.twig' %}

{% block title %}TypeAttribut{% endblock %}

{% block body %}
<div class="card elevation-2">
    <div class="card-header">
        <h6 class="card-title">Visualisation d'un(e) TypeAttribut</h6>
    </div>
    <div class="card-body">
        <dl class="row">
                            <dt class="col-sm-6 text-right">Id :</dt>
                <dd class="col-sm-6 text-secondary">{{ type_attribut.id }}</dd>
                            <dt class="col-sm-6 text-right">Nom :</dt>
                <dd class="col-sm-6 text-secondary">{{ type_attribut.nom }}</dd>
                    </dl>
    </div>
    <div class="card-footer text-center d-flex justify-content-center">
        <a class="btn btn-outline-secondary mr-2" href="{{ path('type_attribut_liste') }}">Retour</a>
        <a class="btn btn-outline-warning mr-2" href="{{ path('type_attribut_modifier', {'id': type_attribut.id}) }}">Modifier</a>
        {{ include('type_attribut/_delete_form.html.twig') }}
    </div>

{% endblock %}
